# Troubleshooting

## Import errors
Ensure the virtual environment is active and dependencies are installed.

## No findings appear
Scan a directory with TODO markers or test secrets.

## Test failures
Run `PYTHONPATH=src python -m pytest -q` from the project root.
